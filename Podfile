# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'first_build_watch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for first_build_watch

  pod 'Gifu'

  target 'first_build_watchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'first_build_watchUITests' do
    # Pods for testing
  end

end

target 'first_build_watch WatchKit App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for first_build_watch WatchKit App

end

target 'first_build_watch WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for first_build_watch WatchKit Extension

end
